<!DOCTYPE html>
<html>
  <head></head>
  <body style="background: black; color: white; font-family: monospace; cursor: url('https://static-00.iconduck.com/assets.00/cursor-pointer-mouse-icon-512x512-b5oc5bvn.png');">
    <div id="com">
      
    </div>
    <br>
    <input type="text">
  </body>
</html>

<script>
function cons(pw) {
  if (pw != "Console") {
    window.alert("Password per apertura Console errata.");
    return "Password errata";
  } else {
    //Apre la finestra per la console

    // Verifica se la finestra con il titolo "Console" è già aperta
    function isWindowOpen(title) {
      const windows = Array.from(window.top.window);
      return windows.some(win => win.document.title === title);
    }
  }
}
</script>